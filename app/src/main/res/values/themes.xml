<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash theme; the real UI theme is Compose-only (TemplateTheme). -->
    <style name="Theme.Template" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Template.App</item>
    </style>

    <style name="Theme.Template.App" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
