# App-specific ProGuard/R8 rules. The template needs none beyond the
# AGP defaults (proguard-android-optimize.txt) — Hilt, Compose, and
# DataStore ship their own consumer rules. Add rules here only when a
# library's documentation says so.
